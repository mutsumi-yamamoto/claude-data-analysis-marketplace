---
name: data-context
description: 分析コンテキストのヒアリング・更新を行います。「コンテキストを確認して」「背景を整理して」「プロジェクトの目的を記録して」と言われたら使用してください。新規プロジェクトでは最初に実行することを推奨します。
---

# コンテキストヒアリング・更新スキル

## 実行手順

### Step 0: データファイルの確認（最優先）

**ヒアリング開始前に、まずデータを確認する。**

`data/` ディレクトリを確認し、以下の情報を把握する:

| 確認項目 | 確認方法 |
|---------|---------|
| ファイル一覧・形式 | `data/` 配下のファイルをリスト |
| 列名・データ型 | pandas で `df.dtypes` または先頭数行を表示 |
| 行数・列数 | `df.shape` |
| 欠損値の概況 | `df.isnull().sum()` |
| 数値列の基本統計 | `df.describe()` |
| 日付列の範囲 | 最小値・最大値 |
| サンプル行 | `df.head(5)` |

**データがない場合**:
- AskUserQuestion で「分析に使うデータファイルを `data/` フォルダへアップロードしてください」と案内する
- データ収集が必要なら `/data-analysis:data-collect` を提示して終了

**データがある場合**:
- 把握した情報をもとに、以下のセクションへの記入候補を自動推論する:
  - セクション5（スコープ）: データの期間・件数
  - セクション6（データソース情報）: ファイル名・形式・件数・期間
  - セクション9（バイアスリスク）: 欠損パターン・偏りの初期評価
- 推論した内容をユーザーに提示し、ヒアリングへ進む

### Step 1: analysis_context.md の確認

`analysis_context.md` を読み込む。

- ファイルが存在しない → `templates/analysis_context.md` をプロジェクトルートにコピーしてから実施する旨を案内
- ファイルが空・不十分 → Step 2 のヒアリングへ進む
- ファイルが十分に記載済み → 現状を確認し、更新が必要な箇所を確認する

### Step 2: ヒアリング（AskUserQuestion を使用、一度に2〜3問）

**Step 0 で把握したデータ情報を提示しながらヒアリングを行う。**
データから推測できる内容は「おそらく〜ではないですか？」と仮説として提示し、ユーザーに確認・修正してもらう形を取る。

**第1回 — ビジネス背景の理解**
- この分析を依頼した（または行う）背景・きっかけは何ですか？
- 分析結果を誰が・どのような意思決定に使いますか？

**第2回 — 目的と成功基準**
- 分析で明らかにしたいことを一文で表すと？（データの列構成から候補を提示する）
- 「分析が成功した」と言えるのはどのような状態ですか？（定量的な基準があれば）

**第3回 — スコープと制約**
- 分析対象の期間は？（データから読み取れた期間を提示して確認）
- 期限・使える技術・予算等の制約はありますか？

### Step 3: analysis_context.md の更新

ヒアリング結果 **と** Step 0 のデータ調査結果を合わせて `analysis_context.md` の該当セクションに記入・更新する。
「分析経過メモ」セクションに今回のヒアリング日時・データ概要・ポイントを追記する。

### Step 4: 次ステップの案内

コンテキストが整ったら、次に実行すべきスキルを提示する:

```
【Issue】が明確になったら → /data-analysis:data-define
データがすでにある場合  → /data-analysis:data-explore  ← データアップロード済みなら通常こちら
データ収集が必要な場合  → /data-analysis:data-collect
```

---

**原則**: 「分かりません」は有効な回答。回答が曖昧な場合は具体例を示して再度確認する。
ビジネス理解が不十分なまま分析に進まない。

$ARGUMENTS
